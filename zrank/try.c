#include <stdio.h>
#include <stdlib.h>

int main () {
   int x, y;

   x = abs(10);
   printf("absolutes value of x = %d\n", x);

   y = abs(-15);
   printf("absolute value of y = %d\n", y);

   return(0);
}