#include <stdio.h>

struct Class
{
    char classname[50];
    int totalstudents;
    int female;
    int male;

    if(totalstudents != female + male){
        printf("There is mismatch in total no. of students wr to no")
    }
    /* data */
};


